import 'package:flutter/material.dart';

class FinancesPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Finances')),
      body: Center(
        child: Text('Welcome to the Finances Page!'),
      ),
    );
  }
}